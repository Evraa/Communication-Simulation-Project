# Communication Project [Semester2021]
# Install Dependencies
- make sure you have python3 >3.5
- run:
```bash
$ python3 -m pip install --user -r requirements.txt
$ sudo apt update
$ sudo apt install -y octave octave-communications octave-miscellaneous
```